To access the build only install the "Windows build" folder, everything else is unity stuff. The build folder will have a racist alligators exe file, run that and the game should boot up.
There is not pause menu with an exit button so you will have to manually close it from task bar or task manager.
Thanks for playing
